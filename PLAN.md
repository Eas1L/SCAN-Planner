# SCAN-Planner `3d_real_dog` 分支计划与进展

## 2026-09-07 GitHub 同步核验

- 此前仅存在于本地主机的八个提交已经全部推送到 GitHub 的
  `3d_real_dog` 分支，其中包括临时与稳定障碍分层修复和长时静止验收记录。
- 本地分支、远端分支和外层工作区登记的 SCAN Planner 版本现已一致。
- 本地主机的外层子模块登记已经修复，递归子模块状态检查可正常识别本仓库。

本文件只记录 SCAN-Planner 在 Unitree Go2 三维实机方法中的规划、占据地图和轨迹执行适配。整套传感器与 AgenticNav 启动方式见本地主机外层目录`3DAgenticNavWorkSpace`的同名分支；上游仓库名称仍为AgenticNavWorkSpace。

## 分支边界

- `3d_real_dog` 从已验证的 `real_dog` 创建，独立承载三维实机方法需要的无运动路径查询，不改变二维实机分支。
- 通用上游功能继续从 `main` 评估后合并；未经实机验证的实验性规划修改不直接进入本分支。
- 外层工作区必须固定本分支的已提交版本，不能依赖狗端未提交源码。

## 当前实现

- 使用 Odin 已配准点云和机体里程计维护滑动占据地图与轻量全局规划底图。
- 过滤 Odin 有限零点、机身近场和近地面异常点，避免形成永久虚假障碍物。
- 手动目标的规划高度持续跟随机身高度，不假设机器狗的世界坐标高度恒定。
- 提供 `/scan_planner_node/check_goal` 服务，用执行时相同的全局与局部规划逻辑检查候选目标，但不改变当前执行状态。
- 目标落入障碍物时沿路径回退，并保留默认 0.25 米终点余量。
- 到达目标后清除执行目标并回到等待状态。
- 在 ROS 中发布规划路径、目标点和成功、重试、失败、到达等可视化状态。
- 2026-09-03 新增 `/scan_planner_node/navigate` 正式导航动作接口，由SCAN统一判断几何移动终态。每次结果区分到达请求目标、到达动态调整后的目标、无全局路径、局部重新规划耗尽、取消和内部故障，并返回请求目标、实际执行目标、最终位姿、调整距离和最终误差。
- 动作结果只会在闭环控制器释放最后一段轨迹后发出；`/planning/navigation_active` 覆盖规划、重新规划、执行和安全停车的完整生命周期。原 `/planning/trajectory_active` 继续作为单段轨迹诊断信号，不能再代表完整导航动作是否成功。

## 2026-08-29 同步记录

- 将机器狗部署目录中已经长期运行的规划源码正式纳入本分支。
- 这些修改此前由外层工作区的七个补丁在构建时生成；同步后源码本身与狗端一致，外层补丁仍保留为旧提交的可重建备份，并由构建脚本通过特征检查避免重复应用。
- 同步范围只包括源代码和服务定义，不包括 `build/`、`devel/`、Catkin 生成文件、补丁备份文件或运行日志。

## 验证要求

- 每次修改至少完成编译，并确认 `CheckGoal.srv` 能正常生成。
- 在 RViz 手动发送目标，确认规划成功时显示轨迹并在到达后回到等待状态。
- 分别验证自由目标、障碍物内目标、狭窄区域失败目标和带小幅高度变化的目标。
- 实机运行前记录外层、AgenticNav 和本仓库三个提交号。
- 新动作接口每次修改后必须验证至少四条终态路径：原目标正常到达、占用目标调整后到达、全局路径立即拒绝、局部规划耗尽或取消后安全停车；结果到达前 `/planning/navigation_active` 不得提前关闭。

## 2026-09-04 三维候选路径接口

- `CheckGoal.srv` 在原有校验结果和调整后目标之外新增 `planned_path`，返回当前 SCAN 全局轨迹按时间采样得到的完整、不执行路径。
- 该路径供三维 AgenticNav 把模型提出的最远十米语义目标沿真实规划路径截断为本次最多三米；查询本身不发布轨迹、不改变规划状态。
- 返回路径强制以当前里程计位置开始、以校验后的实际目标结束；失败时路径为空。完成 Orin 独立工作区编译后，再在传感器上电阶段核对坐标系和无运动查询，实际运动测试仍需用户另行确认。

## 2026-09-03 动作接口验证记录

- 在新 Orin NX（AArch64、ROS Noetic、OpenCV 4.2）上完成 SCAN-Planner 全量编译；修正“尚无里程计时拒绝目标”的结果初始化后再次增量编译通过。
- 隔离测试中，在尚无里程计时提交动作目标，服务端确定返回拒绝结果；请求目标与实际执行目标保持一致，调整标记为否、调整距离为零，避免向 AgenticNav 暴露未初始化数据。
- 使用软件运动学模拟器、空白点云和 0.6 米前进目标完成闭环测试：动作以“到达请求目标”成功结束，最终前进 0.545 米，距请求目标约 0.055 米。
- 同一闭环测试中，`/planning/navigation_active` 的观测顺序为“空闲、活动、活动、空闲、空闲”，确认导航动作结果不会在轨迹控制尚未释放时提前返回。
- 本轮尚未让实体机器狗运动；动态目标调整、局部重新规划耗尽和实体急停仍需在后续受控实机实验中分别验证。

## 2026-09-04 静止运行后的错误障碍累积诊断

- 对当前实机栈完成只读在线采样，没有重启、清图、取消目标、解锁安全门或驱动机器狗。20秒内机身平面位移约5毫米、航向变化约0.07度，Odin点云和里程计均稳定在约10.2赫兹，因此位姿漂移和传感器掉线不是本次通道逐渐变为占据的主因。
- RViz默认显示的是`/grid_map/occupancy_inflate`累积膨胀层，不是当前帧点云。当前彩色图和Odin点云都支持约2米处确有玻璃隔断；真正异常的是机器狗与隔断之间本应空闲的0.3至1.8米区域仍保留大量旧占据源。
- 在该前方区域记录到163个原始占据体素。随后连续15秒、153帧过滤点云没有重新观测到其中任何一个体素，但163个体素全部继续存在，没有一个被清除。另一个30秒窗口中，约20%的帧会出现1至4个零散近距离端点，最小距离约0.57米，说明Odin注册点云在玻璃环境前会间歇产生稀疏近场误点；这些误点并非稳定表面。
- 直接根因是当前占据融合只有射线命中与穿越清除，没有体素观测时间、时间衰减或过期清理。现有参数从未知下界开始时约两次命中就能超过0.80占据阈值；后续只有射线精确穿过同一个三维体素才会降低概率。稀疏点云的后续射线通常不会再次穿过旧误点所在体素，因此机器狗静止也不会自然清除。每个旧体素再按0.25米水平半径和上下高度范围膨胀，少量误点最终会在二维规划层连接成大面积障碍。
- `cloud_raw_confidence_threshold=35`只在发布原始点云时生效；当前关闭`senddtof`并直接使用不带置信度字段的`cloud_slam`，所以这个阈值没有过滤进入SCAN的注册点云。当前桥接节点只按机身半径和高度裁剪，也没有时间一致性检查。
- 代码还存在一个独立的次要缺陷：`raycast_num_`、`flag_rayend_`和`flag_traverse_`使用8位`char`。Orin的AArch64编译器把普通`char`作为无符号类型，约10.2赫兹时编号每256帧、约25秒循环一次；编号碰撞的一帧可能把旧标记误认为本帧标记，减少自由空间射线更新。它应修复，但只影响周期性的一帧，无法单独解释连续15秒完全无当前证据的163个旧体素长期保留，因而不是主根因。
- 后续修复应优先加入“当前证据的时间一致性过滤”和“已占据体素的观测年龄/衰减”，并保证稳定墙面持续刷新、未获支持的孤立点自动消退；同时把射线编号改为不会短周期循环的整数。单纯冷启动清图、降低膨胀半径或放宽规划阈值只能暂时改变显示或安全边界，不能消除累积机制。本节只记录诊断结论，尚未修改融合算法和运行参数。

## 2026-09-04 错误障碍累积修复

- 按确认后的组合方案修改占据融合，但不改路径搜索、轨迹优化、闭环控制和运动安全门。新增的旧障碍处理默认关闭，仅由三维实机启动配置显式启用。
- 地图不做全局定时删除。只有当前自由空间射线从旧占据体素的0.08米邻域通过，而且该体素连续20次地图更新没有得到端点支持时，才按原有一次“未命中”概率逐步降权；当前持续观测到的墙面会刷新时间，暂时遮挡且没有自由射线经过的障碍也不会被删除。
- 将射线终点、射线穿越和旧障碍邻域的逐帧去重标记统一改为32位无符号编号，消除AArch64上原8位编号约25秒循环一次的问题；地图清空、滑动复用和机身足迹清理时同步重置新增状态。
- 已在8GB Orin的独立三维工作区完整编译通过。冷启动静止实测中该功能按配置启用，前方0.3至1.8米空闲区域的原始占据体素由修复前长期残留的163个降至6个；当前真实墙面仍保留。一次仅调用候选检查服务的前方1米目标成功返回135个位姿的无碰撞路径，导航活动状态保持为假，最终速度保持为零。初始验证期间SCAN约占210MB常驻内存，整机仍有约3.4GB可用内存。运动安全门始终关闭，尚未做实体运动验证。

## 2026-09-04 延长静止复查

- 同一次冷启动继续静止运行约18分钟后，前方0.3至1.8米空通道重新出现142至154个原始占据体素，而对应当前过滤点云仅有12至13个点；异常主要集中在前方0.8至1.8米。三次相隔15秒的地图采样结果基本不变，说明这些不是当前表面，而是没有被清除的历史占据。真实墙面在前方1.8至2.8米范围仍稳定保留约699至717个占据体素。
- 对前方0.8至1.8米的98个残留占据体素计算它们到本帧全部自由射线的最近三维距离：没有任何一个落在现配置0.08米内；0.15米只能覆盖29个，0.20米覆盖71个，0.30米覆盖97个。日志虽持续报告每两秒约8至26个自由射线邻域触发降权，但现有0.08米邻域覆盖不到这批射线间隙中的残留体素，因此清理机制实际没有触达主要异常区域。
- 同一前方1米只读规划检查已由冷启动初期的成功退化为“无法生成无碰撞局部轨迹”，返回路径为空；导航状态仍为假、速度为零且运动门关闭。结论是本轮修复只验证了启动初期清图和真实墙面保护，长期静止累积问题仍未解决。下一版不能只机械放大清理半径，因为过大的邻域可能误删邻近自由射线的真实障碍；应让当前射线形成具有传感器角分辨率依据的自由空间覆盖，并结合近期端点保护或占据体素缺少支持的连续帧计数，再做受限降权。

### 下一版修复设计依据

- 进入SCAN的`cloud_slam_filtered`同时承担障碍命中与自由空间射线，但桥接层先按机身相对高度`[-0.30, 0.35]`米裁剪，约3.6万个Odin当前帧点只剩约5200个。这一窄带适合作为碰撞障碍端点，却不适合作为完整自由空间观测；大量地面、墙面其他高度和天花板点被删除后，射线之间留下足以容纳历史误点的空隙。
- 同一时刻对照计算证明应拆分两类证据：约5167条窄带射线对192个前方残留体素在0.08米范围内覆盖为0；约33939条不含无效零点、且不超过5米的原始全高点云射线覆盖192/192，最近距离最大仅约0.011米。把全高点云按方位角和俯仰角各2度保留每个方向最近端点后只需约2351条射线，仍覆盖当时224/224个残留体素，最大最近距离约0.046米；3度分箱约1072条也覆盖224/224，最大约0.064米。
- 推荐保持现有碰撞高度窄带和时间一致性点作为“障碍命中”，另生成经过最近距离角度分箱的全高“只清除射线”。后者只为射线路径提供旧障碍缺少支持的证据，不把地面或天花板端点写成障碍，也不把无回波方向假定为空闲。每条只清除射线在端点前保留安全距离；旧占据体素还必须满足近期没有障碍端点支持、连续多帧被只清除射线覆盖，才按一次未命中逐步降权。这样可以填补当前射线间隙，同时保护端点表面、遮挡后区域和暂时不可见障碍。
- 不推荐把现有半径直接改成0.20至0.30米、做全局时间删除、周期清图或减小碰撞膨胀。这些办法虽然可能恢复当前通道，却分别会扩大误清范围、删除遮挡障碍、丢失地图记忆或削弱实体安全边界。现有两帧/三帧点云入口过滤先保持不变，用上述清除证据修复单独验证；若仍有新误点进入，再基于录制点云比较三帧/五帧或孤立点与稠密表面的差异化准入，避免同时改变两套机制而无法归因。

### 与二维实机配置及窄高度缺失衰减的复核

- 二维`real_dog`分支没有历史障碍衰减半径。用户记忆中的0.80米主要来自桥接层`base_filter_radius=0.80`，它在点云进入SCAN前删除机身水平距离0.8米内的点，并不清除地图中的历史占据；另一个`near_ground_filter_range=0.80`只针对近地面斜坡异常点，而且当前世界坐标点云路径不会进入该条件。二维足迹硬清理使用半径0.25米、前后偏移0.18米和0.03米余量，水平最大前后范围约0.46米、左右约0.28米，也不是0.8米。
- 对当前窄高度过滤点云连续采集20秒、206帧：前方0.8至1.8米空通道中没有出现任何一个0.10米点云体素；前方1.8至2.8米真实墙面有256个体素，其中173个出现在至少100帧，122个连续出现至少100帧。该差异说明本现场问题可以优先用窄高度点云自身的“近期支持消失”处理，不必首先引入全高度原始点云。
- 直接按缺失时间删除仍不安全，因为点未出现也可能是遮挡、采样间隙或暂时超出可见范围。推荐为窄高度点云建立0.10米空间容差的短期支持状态：新障碍仍立即进入规划层以保证安全，但只有连续多帧或邻域稠密支持后才成为稳定障碍；尚未稳定的点连续数帧消失后，按现有未命中概率逐步降权；稳定障碍不因短期缺失而删除，仍由正常射线清除。支持判断不能直接按0.05米地图体素精确匹配，否则点云抖动跨越地图体素边界会把真实墙面误判为消失。
- 因此第一优先方案调整为“窄高度点云的临时障碍与稳定障碍分层”，全高度只清除射线保留为后备方案。先单独验证短暂误点能否自动退出而真实墙面保持；只有长期重复误点被错误晋升为稳定障碍时，才增加全高度可见性证据或更严格的时间过滤。

## 2026-09-04 临时与稳定障碍分层实现

- 已按上述第一优先方案实现可选的临时障碍状态，默认关闭，只由三维实机启动配置显式启用。新占据体素仍立即进入原始占据层和膨胀层，不降低单帧障碍的即时避障能力。
- 当前窄高度点云的真实命中端点会在0.10米空间容差内为占据体素提供本帧支持。临时障碍连续五帧得到支持后晋升为稳定障碍；若尚未稳定便连续五帧没有支持，则从第五帧起每帧施加一次原有未命中概率更新，直到退出占据状态。稳定障碍不使用这种仅凭缺失的衰减，仍只由原有自由射线等机制清除。
- 新增状态在地图整体清空、滑动地图复用、局部单元清理和机身足迹清理时同步复位；32位射线帧编号最终回绕时也同步清理支持标记。运行日志每两秒汇报跟踪体素、临时体素、稳定体素、晋升和衰减数量，便于实机判断是否误删真实墙面或让噪声错误晋升。
- 当前主机缺少ROS Noetic，静态差异检查已经通过，但有效编译必须在8GB Orin的原生ROS环境完成。旧实机程序已经正常停止，后续同步、编译和静态验证期间继续保持运动安全门关闭，不发送导航目标。

## 2026-09-04 临时与稳定障碍长时静止验收

- SCAN提交`8eb6d4e`已同步到8GB Orin的独立三维工作区并完成全量编译。测试使用外层提交`33eb394`，只启动计算端的三路RealSense、Odin和SCAN，不启动旧Nano控制端，不打开运动门，也不发布导航目标。
- 同一次冷启动静止运行从19:44:20持续到20:06:02，约21分42秒，已经超过旧实现约18分钟后重新累积的复发窗口。前方左右0.8米范围内，0.8至1.8米空通道的原始占据在约7分钟、14分钟和21分钟复查中始终为0；0.3至1.8米较宽统计为4至7个，而当前窄高度点云持续看到其中15至21个近场侧缘点，不属于已经消失的历史残留。
- 前方1.8至2.8米真实墙面在上述复查中稳定保留约441至466个原始占据体素，21分钟时为453个；对应当前过滤点云仍有1604个点。运行日志持续同时存在约136至215个临时体素和约1810至1874个稳定体素，并有少量临时体素降权、清除，说明状态机持续工作且没有把真实墙面误删。
- 约14分钟和21分钟时分别调用一次不执行运动的前方1米`check_goal`，两次均成功返回135个位姿，目标未调整；`navigation_active`和`trajectory_active`始终为假。最终传感器频率为Odin原始点云、过滤点云和里程计约10.33赫兹，三路彩色图均6.00赫兹，占据图2.00赫兹，规划俯视图约2.17赫兹。
- SCAN进程约占226MB常驻内存和32%单个处理器核心，整机仍有约3.7GB可用内存，最高温度约60摄氏度。启动瞬间出现一次RealSense深度流硬件警告，随后三路持续稳定；本轮内核日志没有USB断开、复位或描述符错误。正常停止耗时8秒，退出状态为0，随后无传感器、Odin或SCAN残留进程，三台RealSense和Odin均仍可枚举。
- 本场景下现有“连续支持5帧、连续缺失5帧、0.10米容差”无需调整，已经明显优于旧实现的长期静止结果。结论范围仍限于当前固定姿态和玻璃隔断场景；稳定障碍的遮挡恢复、机器狗移动后的滑动地图，以及真实动态障碍仍需在另行授权的受控运动测试中验证。

## 2026-09-09 `CheckGoal`路径语义缺陷

- 14:21:29实机任务证明`CheckGoal`虽然调用`reboundReplan`验证局部可行性，却把`global_data_.global_traj_`采样为`planned_path`返回。`planGlobalTraj`只以起点、目标及直线插值点构造平滑参考线，不是避障路径，因此上层看到的绕行比例总是接近1。
- 第2步候选检查报告3.520米路径对3.516米直达距离，动作下发后首条`optimal_list`实际为6.507米对2.944米，比例2.210，最终局部重规划耗尽；第5步检查报告9.063米对9.060米，动作后的首条实际轨迹为6.050米对3.018米，比例2.005。当前1.60倍/额外1.50米门槛若应用于真实路径应当拒绝。
- 修复需让无运动检查返回实际局部样条，并明确远目标与当前规划视野的边界；执行FSM每次成功重规划后还需对将发布的局部轨迹应用同一绕行门槛，防止地图变化或随机重试在目标下发后产生长绕行。尚未修改规划代码、参数或运行状态。

## 2026-09-09 局部真实路径和执行期绕行保护

- `CheckGoal`成功后改为采样本次`reboundReplan`实际生成的局部B样条，不再返回直线全局参考轨迹；若局部轨迹没有到达本次请求终点、路径超过直达距离1.60倍或额外超过1.50米，服务直接返回失败并附带路径、直达距离、比例和额外距离。
- 正式动作的首次规划、正常重规划和碰撞触发的安全重规划都在发布B样条前执行同一判断。超限轨迹不会发布，也不会进入原有五十次盲目重试；规划器立即发布安全停车轨迹，以“没有可接受局部路径”结束当前动作，并在RViz显示绕行拒绝。
- 新门槛默认关闭，只由三维实机启动文件显式设为1.60倍和1.50米，其他SCAN使用方式保持原行为。修改已在8GB Orin的ROS Noetic环境完整编译通过，包含`scan_planner_node`和计算端桥接包；编译期间没有启动ROS栈或发送运动目标。

## 2026-09-09 终点净空与上层吸附阈值冲突

- 15:57:46任务首批1、2号目标在膨胀占据图内。`adjustGlobalTargetIfOccupied`先沿全局参考路线退到第一空闲点，再额外保留`terminal_clearance=0.25`米，得到相对原目标0.75米和0.33米的安全终点；两条真实局部路径分别为1.90米和2.08米，绕行比例都约为1.00，SCAN正确返回成功。
- AgenticNav随后把该安全终点当作仿真导航网格的“吸附结果”，仍以0.25米总偏移为上限，导致两条SCAN已验证的路径被拒绝。15:54:50任务也复现：原目标只深入占据边界约0.03米时，叠加终点净空后的0.28米总偏移仍超过上层阈值。该问题属于跨接口语义不一致，不是SCAN路径生成失败；本轮未修改规划器。

## 2026-09-09 执行期绕路限制开关

- SCAN保留1.60倍和额外1.50米两个原有阈值，但每次校验准备发布的局部样条时先读取全局参数`/agenticnav/safe_mode`。参数为真才执行绕路拒绝；普通启动为假时只验证轨迹存在并返回路径统计，不因绕路比例或额外距离拒绝。
- 这一运行开关覆盖只读候选检查、首次动作规划和运动中的重新规划，避免上层关闭而执行期仍然开启的状态分裂。碰撞检测、局部轨迹生成、紧急停车、终点净空和动作终态协议保持原样。
- 修改以提交`a5394cd`推送并部署到Orin，`scan_planner_node`和计算端桥接工作区完整编译通过。启动文件离线展开确认参数默认值为假，编译后的节点包含同一参数路径；本轮没有启动传感器栈、打开运动门或发送导航目标。

## 2026-09-12 共享闭环控制器的静止起步朝向故障

- 二维实机任务`20260912_214102_252930`确认，VLM目标和SCAN轨迹正确，但闭环控制器在长距离右转轨迹的静止起点选择了相反朝向。第15号样条前0.8秒的位移只有约0.0069米，低于现有0.01米阈值，代码随即采用约`(-0.008,+0.007)`米每秒的零速残余值，把正确的约负83度方向算成约正137度并输出最大正角速度。
- 原始SCAN速度与运动门后的速度一致，YifanLIO原始与统一位姿一致且高度稳定；问题位于二维和三维分支完全相同的`estimateDesiredYaw`实现。教程包也带有同一逻辑，它来自最初提交`f121613`。建议改为沿样条按空间距离选择前方点，避免用近零瞬时速度决定起步转向；本轮只记录诊断，没有修改控制器。
- 对照15:38的Odin成功录包后确认，同一控制器和同一回退分支以前确实已在运行。Odin一次慢起步的0.8秒位移为0.0081米，但起点速度仅约0.001米每秒，低于0.01米每秒阈值，因此保持当前朝向并正常执行；Yifan任务起点速度为0.01079米每秒，只高出阈值0.00079米每秒，却被作为方向使用。隐藏问题由两种定位在静止速度残余上的差异触发，不是近期修改了规划器。
- 按用户确认采用最小范围修复：保持0.01米前视位移阈值，只把起点速度方向阈值从0.01米每秒提高到0.03米每秒，并分别命名两个阈值。该修改会过滤最新任务中0.01079米每秒的静止残余速度，不改变规划轨迹、速度限制或其他保护逻辑。
- 修改已在新Orin的三维工作区完整编译，`closed_loop_controller`目标重新编译和链接成功；构建期间没有启动ROS运行栈或打开运动门。

## 2026-09-13 手工目标三维高度

- 新增默认关闭的`fsm/manual_goal_use_requested_height`参数；开启后，候选预检查与正式动作都采用请求中的机体中心高度，不再覆盖成当前里程计高度。
- 三维实机启动文件将该参数打开；其他SCAN启动方式继续沿用当前高度。开启时也不再使用旧的负一米目标高度哨兵，以便合法的下楼目标通过。
- 目标占据调整、轨迹生成、碰撞检查、终点净空和运动控制均保持原逻辑，未增加新的合理性或安全检查。
- Orin完整重新编译通过；不连接运动控制的实际节点分别接受高于和低于当前机体0.30米的目标，均生成65点轨迹并原样保留请求高度。
